M2 — File Management
=====================
Status: IN PROGRESS

Goal: Multi-buffer editing, fuzzy picker, file tree sidebar.

Tasks:
- [x] ms-tree crate — DONE (2026-06-11)
      - Flat-vector visible-rows model, lazy read_dir on
        expand, dirs-first case-insensitive sort.
      - Shows dotfiles + gitignored (user's nvim-tree
        filters config). Deferred: fs watch (notify).
- [x] File tree sidebar — DONE (2026-06-11)
      - ms-term/ui/filetree.rs compositor layer, left
        sidebar (40 cols max), Ctrl-n toggle.
      - Keys (user's nvim-tree setup): j/k/g/G move,
        Enter/l expand-or-open, h collapse-or-parent,
        C / Ctrl-] root-to-node (+history), - root-to-
        parent (+history), Ctrl-o previous root,
        q/Esc/Ctrl-n close. Opens via open_document
        (buffer-aware), closes on open.
      - Deferred: create/delete/rename (a/d/r), find
        current file (Ctrl-f / NvimTreeFindFile),
        keep-open + focus switching, fs watch.
- [x] Buffer management — DONE (2026-06-11)
      - Stash-based multi-buffer: editor.document stays THE
        active doc (disjoint field borrows keep working);
        other buffers live in stashed Vec<StashedBuffer>
        carrying document + cursor/scroll + history + marks
        (undo and marks are per-buffer, vim-correct).
        SlotMap deferred to splits/multi-view work — the
        swap-based model is the zen single-view fit.
      - :b N / :b fragment, :bn, :bp, :bd[!], :ls
      - Buffer picker on Space-b (nucleo, ui/buffer_picker.rs)
      - nvim 'hidden' default: switching off a modified
        buffer allowed; :q refuses while ANY buffer modified
      - :e opens into a new buffer, reuses same-path buffer;
        file picker (Space-p) routes through open_document
- [x] Fuzzy file picker — DONE earlier (nucleo, Space-p,
      .gitignore-aware via ignore crate)
- [x] :e file — DONE (M1g, now buffer-aware)

Key files:
- ms-tree/src/lib.rs (new crate)
- ms-view/src/editor.rs — multi-document state
- ms-term/src/application.rs — tree sidebar rendering